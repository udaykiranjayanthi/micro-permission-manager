{
  "name": "persmission-manager-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && yarn copy-assets",
    "copy-assets": "cp -r src/static dist/static && cp src/manifest.json dist/manifest.json"
  },
  "dependencies": {
    "@types/chrome": "^0.1.1",
    "ts-node": "^10.9.2",
    "tsc": "^2.0.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
